(function (global, factory) {
	typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports, require('react-dom/client')) :
	typeof define === 'function' && define.amd ? define(['exports', 'react-dom/client'], factory) :
	(global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory(global.testUtils = {}, global.client));
})(this, (function (exports, client) { 'use strict';

	function renderIntoDocument(element) {
	    const div = document.createElement('div');
	    return client.createRoot(div).render(element);
	}

	exports.renderIntoDocument = renderIntoDocument;

}));
